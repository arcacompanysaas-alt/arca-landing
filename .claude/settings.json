{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_branchPolicy": "BRANCH POLICY: Only work on 'sandbox' (development) or 'main' (production). Never push directly to main unless explicitly authorized by user. All development must go to sandbox first. See CLAUDE.md for full workflow.",
  "permissions": {
    "defaultMode": "default"
  }
}
